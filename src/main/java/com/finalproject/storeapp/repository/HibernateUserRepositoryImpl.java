package com.finalproject.storeapp.repository;

import org.springframework.stereotype.Service;

@Service("userRepository")
public abstract class HibernateUserRepositoryImpl implements UserRepository {
}
